/* Javascript for SurveyMonkeyXBlock. */
function SurveyMonkeyXBlock(runtime, element, options) {

    if (options.is_for_external_course) {
        $(".courseware.wrapper-course-material, .sequence-nav, .page-header, .sequence-bottom").hide();

    }

    $(function ($) {

    });
}
